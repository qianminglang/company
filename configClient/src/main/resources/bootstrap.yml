server:
  port: 8094
spring:
  application:
    name: client1
  cloud:
    config:
      username: clear
      password: clear
      profile: dev
      label: master
      uri: http://localhost:8093/
