server:
  port: 8093
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/qianminglang/spring-cloud.git
          search-paths: cloudConfig/client1