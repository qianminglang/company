server:
  port: 8087
spring:
  application:
    name: eureka-consumer
  cloud:
    loadbalancer:
      retry:
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://eurekaa:8081/eureka
