server:
  port: 8087
spring:
  application:
    name: eureka-consumer
  cloud:
    loadbalancer:
      retry:
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://eureka-9002.com:9002/eureka
